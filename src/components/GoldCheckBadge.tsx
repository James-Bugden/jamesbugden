import { Check } from "lucide-react";

const GoldCheckBadge = () => {
  return (
    <span className="gold-badge">
      <Check className="w-3 h-3 text-white" strokeWidth={3} />
    </span>
  );
};

export default GoldCheckBadge;
import { type DateRange } from "./analyticsShared";

// Timing sections have been folded into strategy recommendations.
// This component is kept as a no-op export for backward compatibility.
export default function TimingSections({ range }: { range: DateRange }) {
  return null;
}
